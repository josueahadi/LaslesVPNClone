import { defineConfig } from 'vite';

export default defineConfig({
    base: 'https://github.com/josueahadi/LaslesVPNClone.git', // Replace 'your-repo-name' with your actual repository name
    build: {
        outDir: 'dist',
    },
});